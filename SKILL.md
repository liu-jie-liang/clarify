---
name: "clarify"
description: "Clarify user's fuzzy problem into a clean session-opening statement. Invoke in a NEW, CLEAN session when user has an idea but cannot articulate it precisely. Never invoke inside an existing task session."
---

# Clarify

把用户脑子里模糊的问法转成一段干净的新会话开场文本。用户只描述问题，不给指令。先研究全局上下文 → 再追问 → 生成完整 brief → 用户反馈迭代 → 定稿。

## 硬规则

- **研究先行**：读完所有直接+传递依赖文件后才开始追问，禁止跳过研究直接追问
- **一次只问一个问题**，附推荐答案
- **生成后不停止**：用户反馈遗漏或不准确时继续改，不重新追问

## 流程

1. 用户描述问题
2. **研究阶段**：读用户问题中直接提到的所有文件 + 这些文件中引用的依赖文件（如提到规则文件则追读其引用的 skill 文件、教学文件等），以结构化摘要展示发现
3. **追问阶段**：基于研究发现，沿最不确定分支往深走，一个分支走透再切下一个
4. 确认理解一致 → **生成完整 brief**（见下方格式）
5. **迭代修改**：用户反馈遗漏/不准确时继续改，不重新追问
6. 用户说"好" → 结束

## 研究阶段

- 读完用户问题中**直接提到的所有文件**
- 追读这些文件中**引用的依赖**（如 `import`、`include`、规则引用的 skill 文件、配置文件中引用路径等）
- 展示结构化发现摘要（文件清单 + 关键发现）后才开始追问
- **禁止跳过研究直接追问**

## 追问原则

- 模糊词（"搞一下""看看""优化""不对"）追问到具体
- 提及之前事件追问"结果是？"
- 表达矛盾直接指出
- 基于研究阶段发现的上下文追问，不做无准备的泛问
- 不问执行决策类问题（"你想怎么处理？""要不要改？""直接改还是先确认？"），追问只做澄清

## 生成文本格式

「完整 brief」为固定五章节结构，新会话没有本轮记忆，必须把所有上下文打包进去：

```
- 背景（架构原则、关键文件位置、相关上下文摘要）
- 本轮发现的问题（每条附证据：文件名+#L行号）
- 需读的文件（完整路径清单，让新会话知道从哪开始）
- 具体修改（编号分解，含注意事项）
- 为什么（一句话总结修改理由）
```

其他要求：
- 粘贴即开始，不需额外解释
- 任务涉及文档或 skill 编辑时，在文本开头附一行："注：涉及文档或 skill 编辑的任务，自动遵循 user_profile.md 中的注意力原则。"
- 任务涉及 GitHub 操作时，在生成文本末尾附加 "GitHub 凭证：~/.trae-cn/github-credentials.md，直接读取使用。"

## 禁止

- 不执行任务
- 不修改文件
- 不替用户判"要不要做"
- 不跳过研究直接追问
- 不生成一次性文本后就停止（必须等待用户反馈并迭代修改直到用户说"好"）
- 用户说"好"后不追问
- 追问完成后直接生成 brief，不追加执行决策类问题

> 示例见 [EXAMPLES.md](./EXAMPLES.md)
> 路径引用规范见 [CONVENTIONS.md](./CONVENTIONS.md)
